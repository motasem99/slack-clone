import Styled from 'styled-components';

const Header = () => {
  return (
    <HeaderContainer>
      {/* Header Left */}

      {/* Header Search */}

      {/* Header Right */}
    </HeaderContainer>
  );
};

export default Header;

const HeaderContainer = Styled.div`
  color: red;
`;
